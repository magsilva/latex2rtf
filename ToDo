Bugs:
 	Fix line numbering better
	Reset footnote numbering at each new chapter

	Extra paragraph at
		- in misc1.tex, near the end, after the italic text before the comma in:
		{\it missing at random},
		- in qualisex, at source lines 182 after the slanted text in:
		 \item les {\sl voies de plusieurs longueurs\/} sont tr\`es %...(line 182)
		- in qualisex in source line 254 after the footnote before the >> in:
		<<~lâgitimes\footnote{L\'egitimit\'e \`a \'evaluer en chaque cas, le cas
		\'ech\'eant par jugement ou d\'ecision des autorit\'es l\'egales.}~>>~:
		- in proffois, source line 305 after the \"\i{} in:
		cont\"enu et de la mise en page, ma\"is, ma\"\i{}zena, ma\^{\i}zena, b\"ureau, etc.),

	\iint is handled like \int but should be double integral.

	\over needs to be hooked up and implemented

	\input is not working properly.
	need to fix redirection of stdin
	need to support \'{i}
	
	spago issues
		\left{ and \over
		line numbering
	
	misc4.tex
		missing references
		error while processing
		
	essential.tex
		error while processing
		
Features:
	support \hsize
	support ## in macro expansion
	
	Add default page numbering
	
	Add support for a local cfg directory
	Add more complete support for \input
	finish special cases for \char 'xxx

	Add support for \usepackage{fullpage}
	Add support for \usepackage{doublespace}
	Add support for \includegraphics

	Add rendering of equations as in ltx2rtf

	Add support for \mark commands
	
	Add autonumbering of equations using fields
	
	Figure out style stuff in rtf header
	add style support for quote environment
	add style support for enumerate environment
 	add back style support for \section, \subsection

	revise the table code
	Suppress empty column that sometimes appears in tabular environments
	Suppress empty row when a final // is present

	add support for \hspace
	add support for \hline

	Improve spacing before and after \section etc.
	Improve font sizes in \section etc.

	Figure out how much translation is possible when no .aux
	file is present.  
